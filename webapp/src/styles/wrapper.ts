import styled from 'styled-components'

export const Wrapper = styled.div`
  padding-inline: 16px;
  margin-inline: auto;
  width: 100%;
  max-width: 1200px;

  @media (max-width: 1024px) {
    padding-inline: 20px;
  }
`
